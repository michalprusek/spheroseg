# PostgreSQL Database Connection
# Example: DATABASE_URL=postgresql://user:password@localhost:5432/mydatabase
DATABASE_URL=

# Server Port (Optional, defaults to 3001 in server.ts)
# PORT=3001

# JWT Secret (Required for authentication - CHANGE THIS to a strong, random secret)
JWT_SECRET=

# CORS Allowed Origins (Optional - Comma-separated list of allowed frontend URLs)
# If empty or not set in development, allows all origins. In production, list specific origins.
# Example: CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com
# CORS_ALLOWED_ORIGINS= 