import React from 'react';
import SegmentationEditorRedirect from './SegmentationEditorRedirect';

/**
 * This component is a wrapper around SegmentationEditorRedirect
 * It's used to redirect from the old segmentation editor to the new one
 */
const SegmentationEditor = () => {
  return <SegmentationEditorRedirect />;
};

export default SegmentationEditor;
